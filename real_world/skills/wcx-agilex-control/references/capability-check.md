# 控制能力只读检查 · 2026-09-12 16:13:55 北京时间

> 本文为动作前的只读检查快照。后续约 10 cm 实机抬升已经完成，见 [重试结果](right-lift-endpoint-20260912.md)；不要将下表历史“未验证”当作当前结论。

后续状态：16:30 经用户授权通过 5556 做过右臂分段试验，因起步跟踪偏差中止；16:36 已无动作客户端。下面保留本次只读检查时的结论，最新证据见 [右臂试验分析](right-lift-20260912.md)。官方资料进一步确认高随动下速度比例无效，不能把默认 100 当成运动速度保证或单独归因为失败原因。

用户只要求检查能力，没有恢复动作授权。本次仅读取服务进程、`/proc` 的 socket 表和 Python 源码，没有连接 5556/5557、实例化机器人客户端或发送动作/使能/停止指令。

## 结论

已具备观测读取、历史离线规划和理解现有动作接口的能力。5556 服务端存在，动作到左右前臂的代码路径已核对；尚不能把这一结论写成“可靠实机控制已经验证”。本任务没有通过该服务执行过经反馈验证的动作，先前直接控制的通信异常也不能仅因接口在线就认定解决。

## 当前服务与协议

- PID 19216，Python `/home/agilex/miniconda3/envs/evorl-ljy/bin/python3.11`；`robot.type=bi_piper_follower`。
- 命令行左臂 `can_left`、右臂 `can_right`，两侧 `require_calibration=false`，频率 30。
- 将该进程 socket inode 与 `/proc/19216/net/tcp` 对照，确认 127.0.0.1 的 5555、5556、5557 均处于 LISTEN。检查端口表不需要连接动作端口。
- `robots/robot_io/server.py:83` 接收 pickle 字典中的 `action`，可带 `t_client`；主循环在获取并发布观测后调用 `robot.send_action()`。
- `robots/bi_piper_follower/bi_piper_follower.py:151` 按 `left_` / `right_` 分发字典键，随后依次调用两侧 `send_action()`。
- 每臂关节键为 `joint_1.pos` 至 `joint_6.pos`，需六个齐全才发关节命令；部分关节键被忽略。完整外层键加左右前缀。`gripper.pos` 独立处理。
- 原始关节单位为度，夹爪为 mm；底层转换为整数千分之一单位。动作是否是原始目标还取决于校准分支，见下节。

上述源码路径均相对于 `/home/agilex/evorl-ljy/evorl/src/lerobot/`。这次静态阅读没有导入或运行这些模块。

## 接入之前需要明确的行为

1. **观测值不能直接假设为保持动作。** `piper_follower.py:207` 返回原始关节反馈；`:138` 只有“未校准且不要求校准”时走透传。即使命令行 `require_calibration=false`，若已加载有效校准，`:227` 仍将动作当成零点偏移并处理方向/范围。本次未检查运行中对象的校准内容，不能判定实际采用哪个分支。
2. **空动作也不保证无硬件写入。** 双臂包装器总是调用左右两侧 `send_action()`；每侧在检查动作键之前先执行模式刷新逻辑。`:196` 到达刷新间隔可发送 `MotionCtrl_2`。所以不能用空字典或“只含左臂键”作为完全无副作用的测试。
3. **默认配置不是小幅低速执行器。** `config_piper_follower.py:38` 默认速度比例 100、`high_follow=true`、模式刷新间隔 1 秒；本次命令行没有看到覆盖。这是源码及启动参数的推断，没有直接读取进程内配置对象。未发现该发送函数内的速度/加速度轨迹生成、碰撞检查或跟踪误差监测。
4. **发送返回不等于执行回执。** `client.py:143` 返回的是发送前归一化动作；服务端丢弃 `robot.send_action()` 返回值，不向动作客户端发送执行确认。需要另行用实际反馈验证跟踪、目标到达与保持。
5. **动作时间过期过滤不是失联保护。** `server.py:98` 取走最新动作后清空缓存，动作之间可被后到的数据覆盖；可选 `stale_action_timeout_s` 默认 None。即使开启，也只是拒绝旧消息，不会在客户端断开后自动停止机械臂。当前主循环还共享观测采集耗时，不能将配置 30 Hz 视作严格实时保证。

## 能力边界

| 能力 | 证据层级 |
|---|---|
| 三路相机观测 | 已实测成功 |
| CAN / ROS 反馈解释 | 已有历史实测与解码回归测试 |
| 小幅抬升 FK / IK | 历史离线计划；不等于实机验证 |
| 当前 5556 动作路径 | 监听状态与源码已核对；没有动作试发 |
| 可靠运动、保持及恢复 | 尚未验证成功 |

后续明确恢复动作任务后，优先核对现有服务的控制权、有效校准与模式，准备带反馈监测的单臂小幅验证。当前不执行这些动作，也不另外接入 SDK 抢占已运行的控制路径。
